import 'package:flutter/material.dart';

// Theme constants

const gTextColor = Color(0xff535353);
const gTextLightColor = Color(0xffacacac);
const gBorderColor = Color(0xffacacac);

const gDefaultPadding = 20.0;
const gDefaultMargin = 20.0;

const minSizeIconButtons = 7.0;

// System constants

final emptyFuture = Future<void>(() {});
